# CodeMerge Release Notes

本次发布聚焦于专业桌面工作区重构、后台复制链路和大规模输入 / 树渲染性能。

## 更新内容

- 主界面调整为面向桌面的增强三栏工作区：左栏输入与处理、中栏任务状态与完整活动、右栏结果与规则；最低完整支持窗口为 1180×720。
- 左栏默认 300px、可调 280–360px；中栏默认 280px、可调 260–340px；右栏至少保留 560px。开始与取消固定在中栏同一位置。
- 持久规则改为右栏一级页签，单击即可到达；完整活动常驻中栏。清空、重置和超大复制继续使用标准 Dialog，移除双击同一按钮确认。
- 运行和取消期间统一锁定来源、临时/持久规则与输出选项，Store reducer 同时拒绝绕过 UI 的 Draft mutation；成功仅清理本次临时规则，取消和失败保留。
- `ProcessResult` 改为 `Arc` 共享并携带合并文件字节数，完成、树、预览、复制和导出不再深拷贝结果，渲染阶段不再读取文件 metadata。
- 新增受 `TaskSupervisor` 管理的分块 Copy job：32MB 内直接后台复制，超过 32MB 先确认，支持进度、取消、失败重试和陈旧 revision 隔离。
- 结构树与文本树共享过滤投影；文本树改为虚拟列表并支持预检数据，树搜索采用 75ms latest-wins 防抖。
- 输入面板按 Store revision 缓存大列表快照；活动投影按执行 revision、筛选和展开项缓存，最多展示 1000 条记录并保持虚拟化滚动。
- 新增工作区快捷键：开始、取消、聚焦搜索、复制活动结果、导出和切换到规则；Dialog / 临时浮层的 Escape 关闭会恢复原焦点。
- 旧临时目录清理移到首帧后的 Maintenance job，`.gitignore`、规则导入导出和文件 metadata 读取移到后台执行器。
- 超长行继续使用 8KB 显示保护，并明确提示复制和导出仍保留完整内容。

- Workspace 重构为单一 `WorkspaceStore` 事务模型，draft、execution、preview 和 navigation 跨域变更由 root reducer 原子提交，陈旧 `JobId` 与陈旧预览版本成为无副作用的 no-op。
- 后台工作统一由 Tokio `TaskSupervisor` 管理，移除生产路径的同步 receiver、固定间隔轮询和混用线程模型；进度按帧合并，取消、失败与终态立即回传。
- Pane 改为按 `ChangeSet` 依赖掩码精确刷新，删除完整结果哈希和 before/after tuple 失效逻辑；活动记录改用有界 `VecDeque`，头部淘汰为 O(1)。
- `AppError` 增加稳定错误码、operation/context 与完整 source chain，processor 与临时文件路径不再返回 `Result<_, String>`；生产代码启用 unwrap/expect Clippy 禁止门禁。
- 结果文件导出改为后台 I/O，按钮会显示保存中、成功或失败状态，慢磁盘与网络盘保存不再阻塞 UI 线程。
- 文件夹预检结果会显示在可搜索、可展开的目录树中，并支持精确逐项排除；输入区保留手动添加文件的移除能力，也可单独清除当前文件夹且不影响单独选择的文件或本次合并临时规则。
- 预览表缓存与 table delegate 改为共享筛选行集合，移除 `ResultModel` 中的完整预览行副本，降低大规模筛选与同步时的内存复制。
- 修复任务状态与反馈链路：取消先进入等待终止状态，扫描可及时停止，迟到事件与旧任务事件不再覆盖当前状态；大批量进度、失败统计和终态耗时保持准确，后台通道异常会转为可诊断错误。
- 全部候选内容读取失败时不再误报完成；处理完成、部分失败、取消和异常现在会主动通知。快速连续保存设置时仅最新 revision 可以落盘并更新告警状态。
- 处理中的临时目录改由生命周期守卫持有，正常取消、失败、latest-wins 替换或工作区强制终止任务时都会立即回收未完成产物。
- 进一步修复点击“合并后内容”后应用无响应的问题：移除内容页入口和失效键计算中的大对象克隆/全量哈希，避免大结果下切换内容页时卡死。
- 左侧输入面板新增“本次合并规则”，支持手动添加目录 / 扩展名规则或导入 `.gitignore`，规则会参与预检和本次处理，成功后自动清空且不会写入配置。
- 新增大结果元数据场景下的内容页切换回归测试，覆盖真实用户路径下的响应性问题。
- 修复“合并后内容”预览在大结果下可能直接无响应的问题，恢复为虚拟列表渲染，避免一次性把整份合并文本灌入编辑器控件。
- 修复预览表与目录树在程序性同步选中时的重入链路，避免打开内容页或同步树选中时出现卡死。
- 重构主界面信息架构，拆分为输入区、状态区、结果区和规则管理区。
- 新增 Windows / macOS 自定义窗口标题栏，统一品牌区与状态胶囊；Linux 不支持 client-side decorations 时会自动退化为紧凑页内 header。
- 标题栏新增版本号显示与 GitHub 开源仓库入口，便于核对版本和访问项目主页。
- 明确桌面三栏的最小窗口约束，避免在非支持尺寸下继续压缩关键操作区。
- 将黑名单管理移出首页主流程，支持单条删除与更安全的危险操作确认。
- 优化处理中状态展示，增加摘要卡片、明确状态层和最近活动列表。
- 统一结果区文案与表头本地化，移除预览区面向最终用户无意义的调试信息。
- GitHub Release workflow 改为读取仓库内的 `RELEASE_NOTES.md` 作为发布说明。
- 修复 Linux `.deb` 安装后缺少桌面启动器的问题，现会一并安装 `.desktop` 文件和应用图标。
- 新增 macOS CI / Release workflow，按 `amd64` 与 `arm64` 产出 `CodeMerge.app` 的 `.zip` 和 `.dmg`。
- 新增 `.zip` 压缩包中的文本文件展开支持，可参与目录树、预检、预览和单文件合并。

## 构建产物

- Linux amd64: `.deb` 与 `.tar.gz`
- Windows amd64: `.zip` 与安装包
- macOS amd64 / arm64: `.zip`（`CodeMerge.app`）与 `.dmg`
- 修复处理结果临时目录清理不完整的问题，结果切换、重置和退出时现在会回收整次处理的临时产物目录。
- 修复超大“合并后内容”预览的并发点击竞态，加载 1MB / 加载全部现在会串行化，请求失败时会保留明确错误提示。
- 调整文件处理并发上限并移除归档元数据的无效计算，降低大批量处理时的额外 CPU 开销。
- 为配置加载/保存失败增加持久告警和重置/重试入口，并将合并输出中的目录结构、文件路径、字符数、Token 标签纳入 i18n。
- 预览表缓存改为基于结果修订号失效，快速滚动与文件列表拖拽也增加了节流，减少跳变和高频刷新。
- macOS 新增 `CODEMERGE_SYSTEM_TITLEBAR=1` 降级开关，可强制使用系统标题栏以规避不稳定环境。
